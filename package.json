{
  "name": "complemento-azure-devops",
  "version": "1.0.0",
  "description": "Complemento para Azure DevOps",
  "main": "index.js",
  "scripts": {
    "mock-api": "json-server --watch db.json --routes routes.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
